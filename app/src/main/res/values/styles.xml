<resources>

    <!-- My application theme. -->
    <style name="AppTheme" parent="Theme.MyTheme">

    </style>

    <style name="Theme.MyTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorControlNormal">@color/divider</item>
    </style>

    <style name="ToolbarTheme" parent="Theme.MyTheme">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="colorControlNormal">@android:color/white</item>

    </style>

    <style name="NavigationViewTheme" parent="Theme.MyTheme">
        <item name="textAppearanceListItem">@style/TextAppearance.AppCompat.Body2</item>
    </style>

    <style name="Theme.CadastroTheme" parent="Theme.MyTheme">
        <item name="colorControlNormal">@color/grey</item>
    </style>

    <!-- Dark Buttons -->
    <style name="ThemeOverlay.MyDarkButton" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorButtonNormal">@color/blue_grey_500</item>
    </style>

    <!-- colorPrimary is used for the default action bar background -->
    <!-- colorPrimaryDark is used for the status bar -->
    <!-- colorAccent is used as the default value for colorControlActivated,
             which is used to tint widgets -->
    <!-- You can also set colorControlNormal, colorControlActivated
           colorControlHighlight, and colorSwitchThumbNormal. -->
</resources>
